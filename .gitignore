# Dependencies
node_modules/
vendor/

# Dev-only sqlite databases (regenerated by `rake db:setup` in the dummy app).
# Tracking these caused CI to skip migrations — the old schema stuck around
# and unique_by: [user_id, session_id] couldn't find a matching index.
spec/dummy/db/*.sqlite3

# Build output
dist/
pkg/
*.gem

# Logs
*.log

# OS files
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Environment
.env
.env.local

# Test coverage
coverage/

# Yarn
.yarn-integrity
yarn-error.log
